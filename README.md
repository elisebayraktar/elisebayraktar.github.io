# Elise Bayraktar

Personal academic website for [Elise Bayraktar](https://elisebayraktar.github.io), built with Astro and deployed to GitHub Pages.

## Development

```sh
pnpm install
pnpm dev
```

Run `pnpm check` to check the project and `pnpm build` to generate the production site in `dist/`.
